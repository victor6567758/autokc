# pgweb connection bookmark (one file per database): the CDC sink that
# receives the replicated tables. Hostname is the docker-compose service
# name, reachable only from inside the compose network - connect via the
# pgweb UI at localhost:8081 and pick this bookmark in the sessions dropdown.
url = "postgres://postgres:postgres@postgres-sink:5432/sinkdb?sslmode=disable"
